-- Optional seed data for a fresh portable install
-- Review before running. These INSERTs are examples and may need to be adapted
-- to match your project's schema.

-- Example: create a minimal admin user (uncomment and edit values if table exists)
-- INSERT INTO users (id, username, email, password_hash, created_at, roles)
-- VALUES (1, 'admin', 'admin@example.local', 'REPLACE_WITH_HASH', NOW(), 'admin');

-- Example: small site settings insert
-- INSERT INTO settings (`key`, `value`) VALUES ('site_name', 'TextSocialMedia');

-- NOTE: This file is intentionally conservative — adapt it to your schema before use.
